<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.matt2393.carreterasnex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppClotoide">
        <activity
            android:name="dev.matt2393.carreterasnex.solucion.SolucionActivity"
            android:label="@string/title_activity_solucion"></activity>
        <activity
            android:name="dev.matt2393.carreterasnex.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppClotoide.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>